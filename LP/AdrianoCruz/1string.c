#include <stdio.h>
int main (void) {

/*vetor de caracteres = cadeia de caracter = string
Para criar um vetor de caracteres é preciso primeiro definir um espaço para armazena-las.
Para isso é precisodeclara o nome o temanho e o tipo de vetor. Sempre deve ser declarada com 1 caracter a mais do que deseja armazenar
poi no final de todo vetor tem um caracter nulo '\0' */

char nome[41];

return 0;
}