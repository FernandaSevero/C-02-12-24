#include <stdio.h>
#include <string.h>
int main(){
   char language[] = "ABC";

   printf("%s", language);
   printf("\nLanguage: %zu \n", strlen(language)); // zu format speifierpara strlen
   return 0;
}