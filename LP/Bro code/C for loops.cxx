#include <stdio.h>
int main()
{
   // for(int index = 1; index <= 14; index+=5)
   //for(int index = -10; index < 1; index++)
   for(int index =1; index > -10; index--)
    {
     printf("%d \n",index);
     }
 return 0;   
}

 /*Índice é um termo usado em programação para se referir à posição de um elemento dentro de uma estrutura de dados, 
 como um array, lista ou string. O índice é utilizado para acessar ou manipular elementos individuais dentro dessa estrutura.

há 3 statements o primeiro o indice em que começa, segundo onde termina, terceiro intervalo "que anda" a cada iteração
*/