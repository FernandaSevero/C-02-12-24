#include<stdio.h>
int main()
{
	/* used to replace a statement where anoperator takes a variable as one of its arguments and the assigns the resul back to the same variable*/
	
	int x = 10;
	
	// x = x + 2;
    // x+=2;
    
    // x = x - 3;
    // x-=3
    
    // x = x * 2;
    // x*= 2;
    
    // x = x / 5;
    // x/= 5;   
    
     // x = x % 5;
    // x%= 5;
      
    printf("%i", x);  
return 0;
}